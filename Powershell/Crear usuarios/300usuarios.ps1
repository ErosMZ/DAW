for(($a=1); $a -le 700; $a++){

    $nombre = Read-Host "Dime el nombre"
    $apellido= Read-Host "Dime el primer apellido"
    $apellido2= Read-Host "Dime el segundo apellido"
    $NIA= Read-Host "Dime el NIA"

}
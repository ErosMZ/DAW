sudo mount -t ext4 /dev/sdb2 /NOTICIAS/particionNoticias/
 sudo mount -t ext4 /dev/sdb4 /BACKUPS/

si quieres crear una particion que no sea guardarlo en una carpeta
hay que creear en /media una carpeta
